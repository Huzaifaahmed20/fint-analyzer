{
  "name": "spend-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Local, CSV-based personal spend tracker. No bank connection, no cloud - everything stays on-device.",
  "main": "server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server.js",
    "test": "node --disable-warning=ExperimentalWarning --test test/*.test.js"
  },
  "dependencies": {
    "csv-parse": "^5.6.0",
    "express": "^4.21.2",
    "multer": "^2.0.1",
    "pdfjs-dist": "^6.3.289"
  }
}
